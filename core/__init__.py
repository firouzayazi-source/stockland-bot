"""
core/ — لایهٔ منطق تازه و نازک، فقط برای api.py (نه بات/پنل)

⚠️ برخلاف تصور اول از اسمش، این پکیج توسط همهٔ کلاینت‌ها استفاده نمی‌شه — فقط
api.py (روتر REST مینی‌اپ/PWA، /api/v1/*) از این استفاده می‌کنه. bot.py و
admin_panel.py مستقیم از db.py استفاده می‌کنن (بدون این لایهٔ میانی). این یه
تصمیم معماری آگاهانه‌ست، نه ناقص‌بودن — قرارداد کامل در CLAUDE.md بخش ۴/۲۱.

مقایسه با iphone_valuation/: اون پکیج برعکسِ اینه — هم بات هم api.py هم پنل
مستقیم ازش استفاده می‌کنن، چون اون فیچر باید هم‌زمان از هر سه مصرف‌کننده در
دسترس باشه.
"""
from . import products, orders, wallet, partners, referrals
